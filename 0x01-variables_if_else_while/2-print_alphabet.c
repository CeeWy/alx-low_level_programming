#include <stdio.h>
#include <stdlib.h>
#include <time.h>
/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
	char lAlp;

	for (lAlp = 'a'; lAlp <= 'z'; lAlp++)
		putchar(lAlp);
		putchar('\n');

	return (0);
}

