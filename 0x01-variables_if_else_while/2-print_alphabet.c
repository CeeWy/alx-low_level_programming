#include <stdio.h>
#include <stdlib.h>
#include <time.h>
/**
 * main - Entry point
 *
 * Return: Always 0 (success)
 */
int main(void)
{
	char lAlp;

	for (lAlp = 'a'; lAlp <= 'z'; lAlp++)
		putchar(lAlp);
		printf("\n");

	return (0);
}
